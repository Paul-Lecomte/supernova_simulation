#include "../include/shader.h"
// Implémentation du chargement, compilation et activation des shaders
Shader::Shader(const std::string& vertexPath, const std::string& fragmentPath) {
    // Charger et compiler les shaders
}
void Shader::use() {
    // Activer le shader
}